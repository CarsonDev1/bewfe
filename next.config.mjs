/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'res.cloudinary.com',
			'beta-api.bachlongmobile.com',
			'be.rongvang.online'
		],
	},
};

export default nextConfig;
